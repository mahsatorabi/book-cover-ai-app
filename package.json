{
  "name": "book-cover-ai-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute bookdb --local --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.26.0"
  }
}
